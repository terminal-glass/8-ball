# bge-large

bge-large 277.5K Downloads Updated 1 year ago Embedding model from BAAI mapping texts to vectors. Embedding model from BAAI mapping texts to vectors. Cancel embedding 335m CLI cURL Python JavaScript Documentation Documentation ollama pull bge-large curl http://localhost:11434/api/embed \
  -d '{
    "model": "bge-large",
    "input": "Why is the sky blue?"
  }' import ollama

response = ollama.embed(
    model= 'bge-large' ,
    input= 'The sky is blue because of Rayleigh scattering' ,
) print (

- Models: 1
- Tags: 3
- Capabilities: embeddings (4), text_generation (4)

Deployment coverage is summarized per model under `data/generated/pages/models/`.