# embeddinggemma

embeddinggemma 1.5M Downloads Updated 10 months ago EmbeddingGemma is a 300M parameter embedding model from Google. EmbeddingGemma is a 300M parameter embedding model from Google. Cancel embedding 300m CLI cURL Python JavaScript Documentation Documentation ollama pull embeddinggemma curl http://localhost:11434/api/embed \
  -d '{
    "model": "embeddinggemma",
    "input": "Why is the sky blue?"
  }' import ollama

response = ollama.embed(
    model= 'embeddinggemma' ,
    input= 'The sky is blu

- Models: 1
- Tags: 5
- Capabilities: embeddings (6), text_generation (6)

Deployment coverage is summarized per model under `data/generated/pages/models/`.