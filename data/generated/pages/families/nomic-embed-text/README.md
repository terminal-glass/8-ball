# nomic-embed-text

nomic-embed-text 80.5M Downloads Updated 2 years ago A high-performing open embedding model with a large token context window. A high-performing open embedding model with a large token context window. Cancel embedding CLI cURL Python JavaScript Documentation Documentation ollama pull nomic-embed-text curl http://localhost:11434/api/embed \
  -d '{
    "model": "nomic-embed-text",
    "input": "Why is the sky blue?"
  }' import ollama

response = ollama.embed(
    model= 'nomic-embed-text' ,
    

- Models: 1
- Tags: 3
- Capabilities: embeddings (4), text_generation (4)

Deployment coverage is summarized per model under `data/generated/pages/models/`.