# C10.3 Canonical Profiles Generation Report

Generated by `scripts/generate-profiles-from-agents.py`.

See `profiles/generated/profile-generation-report.json` for machine-readable counts and
`profiles/manifest.json` for the canonical schema contract.

Legacy exports are retained under `profiles/legacy/` with a migration README.

Regenerate:

```bash
python3 scripts/generate-profiles-from-agents.py
python3 scripts/validate-profiles-from-agents.py
```
