# Non-canonical compatibility exports

Provider assumption JSON files in this directory are retained for existing C10
install helpers and capability validators. They are **not** the canonical C10.3
runtime profile matrix.

Canonical profile data lives under `profiles/<model-slug>/` and is generated by
`scripts/generate-profiles-from-agents.py`.

Removal condition: no install or validator consumer reads these files directly.
